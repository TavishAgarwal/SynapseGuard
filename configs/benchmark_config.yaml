# SynapseGuard Benchmark & Validation Configuration

datasets:
  truthfulqa:
    path: "data/benchmarks/truthfulqa_subset.json"
    sample_count: 100
  halueval:
    path: "data/benchmarks/halueval_subset.json"
    sample_count: 100
  rag_subset:
    path: "data/benchmarks/rag_subset.json"
    sample_count: 100

predictability_inputs:
  path: "data/predictability_inputs/input_spectrum.json"
  total_samples: 1000

psc_score:
  alpha_confidence_weight: 0.5
  beta_entropy_weight: 0.5
  warning_threshold: 0.65
  halt_threshold: 0.85
