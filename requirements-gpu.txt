# SynapseGuard - GPU Session Requirements (RTX 3050 6GB VRAM, WSL2 Ubuntu, CUDA 12.x)
# ONLY run during single GPU extraction session on WSL2
# Pinned exact versions for long-term paper reproducibility
# Note: vllm/vllm-hook retained for reference, but execution uses HuggingFace Transformers
# with forward hooks for activation extraction.

bitsandbytes==0.41.3
sae-lens==1.3.0
transformers==4.40.1
torch==2.1.2
pyyaml==6.0.1
pandas==2.2.1
scipy==1.12.0
numpy==1.26.4
scikit-learn==1.4.2
matplotlib==3.8.4
requests==2.31.0
accelerate==0.29.3
